//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
public class Main {
    public static void main(String[] args) {
        /*BinarySearchTree<Double, Candidate> binaryTree = new BinarySearchTree<>();

        binaryTree.insert(90.0, new Candidate() {
            public double time() { return 90.0; }
            public String namSe() { return "Anna"; }
        });
        binaryTree.insert(92.0, new Candidate() {
            @Override public double time() { return 92.0; }
            @Override public String name() { return "Kati"; }
        });
        binaryTree.insert(91.0, new Candidate() {
            @Override public double time() { return 91.0; }
            @Override public String name() { return "Salme"; }
        });
        binaryTree.insert(70.0, new Candidate() {
            @Override public double time() { return 70.0; }
            @Override public String name() { return "Mati"; }
        });
        binaryTree.insert(100.0, new Candidate() {
            @Override public double time() { return 100.0; }
            @Override public String name() { return "Viktor"; }
        });


        System.out.println(binaryTree.search(90.0, ));
        System.out.println(binaryTree.search(81.0));
        System.out.println(binaryTree.search(70.0));
        binaryTree.delete(92.0);

        List<Candidate> values = binaryTree.inOrderTraversal();

        for (Candidate v : values) {
            System.out.println("Time: " + v.time() + ", Name: " + v.name());
        }

        List<Candidate> rangeValues = binaryTree.findInRange(91.0, 100.0);

        System.out.println("Candidates in range 80.0 - 95.0:");
        for (Candidate c : rangeValues) {
            System.out.println("Time: " + c.time() + ", Name: " + c.name());
        }*/


    }
}